<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pixelfos</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f8f8;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        h1 {
            color: #333;
        }

        table {
            width: 50%;
            margin: 20px auto;
            border-collapse: collapse;
            border: 1px solid #ddd;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #f5f5f5;
        }

        .nft-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .nft-card {
            border: 1px solid #ddd;
            border-radius: 8px;
            margin: 20px;
            padding: 15px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out;
            max-width: 300px;
        }

        .nft-card:hover {
            transform: scale(1.05);
        }

        .nft-image {
            max-width: 100%;
            height: auto;
            border-radius: 6px;
            margin-bottom: 15px;
        }

        h3 {
            color: #333;
            font-size: 1.2em;
            margin-bottom: 10px;
        }

        p {
            color: #666;
            font-size: 0.9em;
        }
    </style>
</head>
<body>

    <h1>Pixelfos</h1>

    <div class="nft-container">
        {{#each nfts}}
            <div class="nft-card">
                <img src="{{this.image}}" alt="{{ this.name }}" class="nft-image">
                <h3>{{this.name}}</h3>
                <p>{{this.description}}</p>

                <table>
                    <thead>
                    <tr>
                        <th>Attribute</th>
                        <th>Value</th>
                    </tr>
                    </thead>
                    <tbody>
                        {{#each this.attributes}}
                            <tr>
                                <td>{{ this.trait_type }}</td>
                                <td>{{ this.value }}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        {{/each}}


    </div>

</body>
</html>
